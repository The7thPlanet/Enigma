

#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <fun/LTN.h>

char *main(char *messageI[] , char *key )
{
    strcpy(char *message[] , messageI[]) ;
      char alpha[] = " abcdefghilmnopqrstuvwxyz"; //Zero-indexed array of letters
    
    //char b = 'b'-'a'+1; //b=2, an unprintable character
    //int c = b; //b=2
    char *NCM;

    for(int CON = 0 ;message==NULL;CON++)

    char NM[] =  ( char* ) malloc( ( CON + 1 ) *sizeof( char ) );

    for(int i = 0 ;message==NULL;i++)
    {
        int NUM = LTN(message[i],alpha);
        itoa( NUM , NM[i],10);
    }
/*
    char *c;
    while(1)
     {
        c = ;
        if(c==EOF)
        {break;}
        else

        {printf("%c", c);}
            
     }
     */
     //return ;
}