#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main(void)
{
    char COMPARISON[26] = "abcdefghijklmnopqrstvwxyz";
    char M[12];
    char df[] ="hello world";
    for (int i = 0;strcmp(df[i],"\0")==0;i++)
    {
        int c = 0; 
        for(int n=0;df[n] == NULL;n++)
    {
        for(int i=0;COMPARISON[i] == NULL; i++)
        {
            strcmp(I[n],COMPARISON[i]);
        }
    }
        itoa(c,M[i],10);
    }
}
